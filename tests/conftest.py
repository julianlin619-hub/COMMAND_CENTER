"""Shared pytest fixtures and configuration.

Currently empty — individual test modules carry their own fixtures. Kept so
pytest has a conftest discovery anchor and so conventional test helpers can
land here as they emerge.
"""
